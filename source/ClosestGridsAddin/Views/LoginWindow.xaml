<Window x:Class="ClosestGridsAddinVANILLA.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BIMS - Iniciar Sesión"
        Height="450" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Resources>
        <!-- Estilos para los controles -->
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#005A9E"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#CCCCCC"/>
                    <Setter Property="Cursor" Value="Arrow"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Background="White" 
            CornerRadius="10" 
            BorderThickness="1" 
            BorderBrush="#DDDDDD">
        <Border.Effect>
            <DropShadowEffect Color="Black" Direction="0" ShadowDepth="0" BlurRadius="20" Opacity="0.3"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header con botón de cerrar -->
            <Grid Grid.Row="0" Background="#007ACC" MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <TextBlock Text="BIMS - Building Information Management System" 
                           Foreground="White" 
                           FontSize="14" 
                           FontWeight="Bold"
                           Margin="15,12"
                           VerticalAlignment="Center"/>
                <Button Content="✕" 
                        Width="35" 
                        Height="35" 
                        HorizontalAlignment="Right"
                        Background="Transparent"
                        Foreground="White"
                        BorderThickness="0"
                        FontSize="18"
                        FontWeight="Bold"
                        Cursor="Hand"
                        Click="CloseButton_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#C75050"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>

            <!-- Contenido principal -->
            <Grid Grid.Row="1" Margin="30,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Logo o Icono -->
                <TextBlock Grid.Row="0" 
                           Text="🔐" 
                           FontSize="48" 
                           HorizontalAlignment="Center" 
                           Margin="0,0,0,10"/>

                <!-- Título -->
                <TextBlock Grid.Row="1" 
                           Text="Iniciar Sesión" 
                           FontSize="20" 
                           FontWeight="Bold" 
                           HorizontalAlignment="Center" 
                           Margin="0,0,0,20"/>

                <!-- Email -->
                <TextBlock Grid.Row="2" 
                           Text="Correo electrónico:" 
                           FontSize="13" 
                           Margin="0,0,0,5"/>
                <TextBox Grid.Row="3" 
                         x:Name="EmailTextBox" 
                         Margin="0,0,0,15"
                         TextChanged="EmailTextBox_TextChanged"/>

                <!-- Password -->
                <TextBlock Grid.Row="4" 
                           Text="Contraseña:" 
                           FontSize="13" 
                           Margin="0,0,0,5"/>
                <PasswordBox Grid.Row="5" 
                             x:Name="PasswordBox" 
                             Margin="0,0,0,15"
                             KeyDown="PasswordBox_KeyDown"/>

                <!-- Mensaje de error -->
                <TextBlock Grid.Row="6" 
                           x:Name="ErrorTextBlock" 
                           Foreground="Red" 
                           FontSize="12" 
                           TextWrapping="Wrap" 
                           Margin="0,0,0,10" 
                           Visibility="Collapsed"/>

                <!-- Botones -->
                <StackPanel Grid.Row="7" VerticalAlignment="Bottom">
                    <Button x:Name="LoginButton" 
                            Content="Iniciar Sesión" 
                            Style="{StaticResource ButtonStyle}" 
                            Click="LoginButton_Click" 
                            IsEnabled="False"
                            Margin="0,0,0,10"/>

                    <TextBlock HorizontalAlignment="Center" Margin="0,10,0,5">
                        <Hyperlink Click="ForgotPassword_Click" Foreground="#007ACC">
                            ¿Olvidaste tu contraseña?
                        </Hyperlink>
                    </TextBlock>

                    <Border BorderThickness="0,1,0,0" BorderBrush="#EEEEEE" Padding="0,15,0,0" Margin="0,10,0,0">
                        <TextBlock HorizontalAlignment="Center" TextAlignment="Center">
                            <Run Text="¿No tienes cuenta?" />
                            <LineBreak/>
                            <Hyperlink Click="Register_Click" Foreground="#007ACC">
                                Regístrate aquí
                            </Hyperlink>
                        </TextBlock>
                    </Border>
                </StackPanel>
            </Grid>

            <!-- Loading overlay -->
            <Grid x:Name="LoadingOverlay" 
                  Grid.RowSpan="2" 
                  Background="#99000000" 
                  Visibility="Collapsed">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="⏳" FontSize="48" HorizontalAlignment="Center"/>
                    <TextBlock Text="Validando credenciales..." 
                               Foreground="White" 
                               FontSize="14" 
                               Margin="0,10,0,0"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
