<Window x:Class="ClosestGridsAddinVANILLA.Views.RegisterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BIMS - Registro de Usuario"
        Height="550" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#28A745"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#218838"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#CCCCCC"/>
                    <Setter Property="Cursor" Value="Arrow"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Background="White" 
            CornerRadius="10" 
            BorderThickness="1" 
            BorderBrush="#DDDDDD">
        <Border.Effect>
            <DropShadowEffect Color="Black" Direction="0" ShadowDepth="0" BlurRadius="20" Opacity="0.3"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Background="#28A745" MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <TextBlock Text="Registrar Nueva Cuenta" 
                           Foreground="White" 
                           FontSize="14" 
                           FontWeight="Bold"
                           Margin="15,12"
                           VerticalAlignment="Center"/>
                <Button Content="✕" 
                        Width="35" 
                        Height="35" 
                        HorizontalAlignment="Right"
                        Background="Transparent"
                        Foreground="White"
                        BorderThickness="0"
                        FontSize="18"
                        FontWeight="Bold"
                        Cursor="Hand"
                        Click="CloseButton_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#C75050"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>

            <!-- Contenido -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <Grid Margin="30,20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Icono -->
                    <TextBlock Grid.Row="0" 
                               Text="📝" 
                               FontSize="48" 
                               HorizontalAlignment="Center" 
                               Margin="0,0,0,20"/>

                    <!-- Nombre completo -->
                    <TextBlock Grid.Row="1" 
                               Text="Nombre completo:" 
                               FontSize="13" 
                               Margin="0,0,0,5"/>
                    <TextBox Grid.Row="2" 
                             x:Name="NameTextBox" 
                             Margin="0,0,0,15"
                             TextChanged="InputField_TextChanged"/>

                    <!-- Email -->
                    <TextBlock Grid.Row="3" 
                               Text="Correo electrónico:" 
                               FontSize="13" 
                               Margin="0,0,0,5"/>
                    <TextBox Grid.Row="4" 
                             x:Name="EmailTextBox" 
                             Margin="0,0,0,15"
                             TextChanged="InputField_TextChanged"/>

                    <!-- Contraseña -->
                    <TextBlock Grid.Row="5" 
                               Text="Contraseña (mínimo 6 caracteres):" 
                               FontSize="13" 
                               Margin="0,0,0,5"/>
                    <PasswordBox Grid.Row="6" 
                                 x:Name="PasswordBox" 
                                 Margin="0,0,0,15"
                                 PasswordChanged="InputField_PasswordChanged"/>

                    <!-- Confirmar contraseña -->
                    <TextBlock Grid.Row="7" 
                               Text="Confirmar contraseña:" 
                               FontSize="13" 
                               Margin="0,0,0,5"/>
                    <PasswordBox Grid.Row="8" 
                                 x:Name="ConfirmPasswordBox" 
                                 Margin="0,0,0,15"
                                 PasswordChanged="InputField_PasswordChanged"
                                 KeyDown="ConfirmPasswordBox_KeyDown"/>

                    <!-- Mensaje de error -->
                    <TextBlock Grid.Row="9" 
                               x:Name="ErrorTextBlock" 
                               Foreground="Red" 
                               FontSize="12" 
                               TextWrapping="Wrap" 
                               Margin="0,0,0,10" 
                               Visibility="Collapsed"/>

                    <!-- Botones -->
                    <StackPanel Grid.Row="9" VerticalAlignment="Bottom">
                        <TextBlock TextWrapping="Wrap" 
                                   FontSize="11" 
                                   Foreground="#666666" 
                                   Margin="0,0,0,15"
                                   Text="Al registrarte, recibirás una licencia de prueba gratuita por 30 días."/>

                        <Button x:Name="RegisterButton" 
                                Content="Crear Cuenta" 
                                Style="{StaticResource ButtonStyle}" 
                                Click="RegisterButton_Click" 
                                IsEnabled="False"
                                Margin="0,0,0,15"/>

                        <TextBlock HorizontalAlignment="Center">
                            <Run Text="¿Ya tienes cuenta?" />
                            <Hyperlink Click="Login_Click" Foreground="#007ACC">
                                Inicia sesión aquí
                            </Hyperlink>
                        </TextBlock>
                    </StackPanel>
                </Grid>
            </ScrollViewer>

            <!-- Loading overlay -->
            <Grid x:Name="LoadingOverlay" 
                  Grid.RowSpan="2" 
                  Background="#99000000" 
                  Visibility="Collapsed">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="⏳" FontSize="48" HorizontalAlignment="Center"/>
                    <TextBlock Text="Creando cuenta..." 
                               Foreground="White" 
                               FontSize="14" 
                               Margin="0,10,0,0"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
